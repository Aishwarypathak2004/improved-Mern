<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit page</title>
    <style>
        /* Reset and base styling */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
}

body {
  background: linear-gradient(135deg, #7dc5ff, #1566b3);
  color: #fff;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 20px;
}
.above{
  display: inline-block;
}
/* Card container */
form {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  padding: 25px;
  border-radius: 18px;
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.25);
  width: 380px;
  display: flex;
  flex-direction: column;
  gap: 15px;
  animation: fadeIn 0.7s ease-in-out;
}

/* Heading */
h3 {
  text-align: center;
  font-size: 22px;
  font-weight: bold;
  margin-bottom: 10px;
}

p {
  text-align: center;
  font-size: 15px;
  margin-bottom: 20px;
}

/* Textarea */
textarea {
  min-height: 120px;
  padding: 12px 14px;
  border-radius: 12px;
  border: none;
  outline: none;
  font-size: 15px;
  background: rgba(255, 255, 255, 0.85);
  resize: none;
  transition: all 0.3s ease;
}

textarea:focus {
  background: #fff;
  box-shadow: 0 0 10px rgba(240, 147, 251, 0.6);
}

/* Edit button */
button {
  padding: 12px;
  border: none;
  border-radius: 12px;
  font-size: 16px;
  font-weight: bold;
  background: linear-gradient(135deg, #43e97b, #38f9d7);
  color: #fff;
  cursor: pointer;
  transition: transform 0.2s ease, box-shadow 0.3s ease;
}

button:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 18px rgba(0, 0, 0, 0.3);
}

button:active {
  transform: scale(0.96);
}

/* Fade-in animation */
@keyframes fadeIn {
  from { opacity: 0; transform: scale(0.9); }
  to { opacity: 1; transform: scale(1); }
}

    </style>
</head>
<body>
   <div class="above"> <h3>Edit your chat: <%=mainchat._id%></h2><br><br>
        <p>This chat was sent from <b><%=mainchat.from%></b> to <b><%=mainchat.to%></b></p>
       <form method="post" action="/chats/<%=mainchat._id%>?_method=PUT">
            <textarea name="msg"><%=mainchat.msg%></textarea>
            <button>Edit</button>
        </form></div>
       
</body>
</html>